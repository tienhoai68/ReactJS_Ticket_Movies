import data from "../../data/listChair.json";

const DEFAULT_STATE = {
    listChair : data,
};
export const bookTickets = (state = DEFAULT_STATE, action) => {
    switch (action.type) {
        // case value:
            
        //     break;
    
    }
    return {...state};
}